---
/* Intentionally renders nothing.
 *
 * The PolyKybd docs deliberately drop the "On this page" table of contents so
 * the content — especially the wide flowchart / sequence diagrams — gets the
 * full width of the page. This stub is registered for BOTH `TableOfContents`
 * and `MobileTableOfContents` in astro.config.mjs, so neither the desktop
 * right-sidebar ToC nor the mobile "On this page" bar is emitted.
 *
 * The now-empty right column is collapsed and the content measure widened in
 * src/styles/custom.css.
 */
---
